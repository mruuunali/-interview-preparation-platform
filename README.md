# PrepIQ — Interview Prep Platform
Java Spring Boot REST API backend.

## Tech Stack
Java 17, Spring Boot 3, MySQL, JPA, JWT, Maven

## Setup
1. Clone the repo
2. Create MySQL database: prepiq_db
3. Update application.properties with DB credentials
4. Run: mvn spring-boot:run
